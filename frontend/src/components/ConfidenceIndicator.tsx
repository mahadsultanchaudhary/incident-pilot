export function ConfidenceIndicator({ value }: { value: number }) { const percent = Math.round(value * 100); return <div className="confidence"><div className="confidence-ring" style={{ '--confidence': `${percent * 3.6}deg` } as React.CSSProperties}><span>{percent}%</span></div><div><strong>Root cause confidence</strong><p>Evidence is consistent across correlated requests.</p></div></div> }
