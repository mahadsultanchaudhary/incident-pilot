import { Cell, Line, LineChart, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import type { DashboardData } from '../types'

const colors = ['#4f8cff', '#f7b955', '#f06b7a', '#65d6a3']
export function FrequencyChart({ data }: { data: DashboardData['frequency'] }) { return <div className="chart"><ResponsiveContainer width="100%" height={210}><LineChart data={data}><XAxis dataKey="day" tickLine={false} axisLine={false} /><YAxis allowDecimals={false} tickLine={false} axisLine={false} width={24} /><Tooltip contentStyle={{ background: '#151d2d', border: '1px solid #2b3952', borderRadius: 6 }} /><Line type="monotone" dataKey="incidents" stroke="#4f8cff" strokeWidth={3} dot={{ r: 3 }} /></LineChart></ResponsiveContainer></div> }
export function ErrorChart({ data }: { data: DashboardData['error_distribution'] }) { return <div className="error-chart"><ResponsiveContainer width={150} height={150}><PieChart><Pie data={data} dataKey="value" innerRadius={42} outerRadius={63} paddingAngle={3}>{data.map((item, index) => <Cell key={item.name} fill={colors[index]} />)}</Pie><Tooltip contentStyle={{ background: '#151d2d', border: '1px solid #2b3952', borderRadius: 6 }} /></PieChart></ResponsiveContainer><div className="legend">{data.map((item, index) => <span key={item.name}><i style={{ background: colors[index] }} />{item.name}<b>{item.value}%</b></span>)}</div></div> }
