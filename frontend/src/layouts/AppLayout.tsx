import type { ReactNode } from 'react'
import { Activity, Bell, Bot, ChevronDown, CircleAlert, LayoutDashboard, Search, Settings, ShieldCheck } from 'lucide-react'
import { NavLink, useLocation } from 'react-router-dom'

const navigation = [{ to: '/', label: 'Overview', icon: LayoutDashboard }, { to: '/incidents', label: 'Incidents', icon: CircleAlert }, { to: '/investigation', label: 'AI Investigation', icon: Bot }, { to: '/settings', label: 'Settings', icon: Settings }]
export function AppLayout({ children }: { children: ReactNode }) { const location = useLocation(); const title = navigation.find((item) => item.to === location.pathname)?.label || 'Incident details'; return <div className="app-shell"><aside className="sidebar"><NavLink to="/" className="brand"><span><Activity size={21} /></span><b>Incident<span>Pilot</span></b></NavLink><nav>{navigation.map(({ to, label, icon: Icon }) => <NavLink key={to} to={to} className={({ isActive }) => isActive ? 'nav-item active' : 'nav-item'} end><Icon size={18} />{label}</NavLink>)}</nav><div className="sidebar-footer"><div className="environment"><i />Production<ChevronDown size={14} /></div><div className="workspace"><ShieldCheck size={18} /><span>Platform Reliability<small>on-call workspace</small></span></div></div></aside><main><header className="topbar"><div><span className="breadcrumb">Operations / {title}</span><h1>{title}</h1></div><div className="top-actions"><label className="search-field"><Search size={17} /><input placeholder="Search incidents, services..." /></label><button className="icon-button" aria-label="Notifications"><Bell size={18} /><i className="notification-dot" /></button><button className="avatar" aria-label="Open user menu">MA</button></div></header><div className="page-content">{children}</div></main></div> }
